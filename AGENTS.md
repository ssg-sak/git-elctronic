# EV SafeCharge — 팀 공통 에이전트 가이드

## 프로젝트 요약

EV SafeCharge는 사용자가 충전소에 **도착했을 때 실제로 충전할 가능성이 높은** 전기차 충전소를 추천하는 서비스다. 핵심 가치는 "가까운 충전소"가 아니라 "충전 성공 확률이 높은 충전소"이며, 이를 위해 실시간 충전기 상태, 상태 갱신 시각(데이터 신뢰도), 교통 반영 이동시간, 충전기 수, 주차·날씨·주변 시설 정보를 종합한다. 전체 기획은 `README.md` 참고.

## 팀 구조와 담당 영역

| 담당 | 디렉터리 | 상세 가이드 |
|---|---|---|
| 프론트엔드 (1명) | `apps/web/` | `apps/web/AGENTS.md` |
| 백엔드 (1명) | `apps/api/`, `packages/recommendation-core/` | `apps/api/AGENTS.md` |
| 데이터 수집 (1명) | `apps/data-pipeline/collection/` | `apps/data-pipeline/AGENTS.md` |
| 데이터 가공 (1명) | `apps/data-pipeline/processing/` | `apps/data-pipeline/AGENTS.md` |

`packages/shared-types/`(공통 타입)는 프론트·백엔드 공동 관리, `docs/`·`infra/`는 전원 공용이다.

**자신의 담당 디렉터리 밖의 코드는 수정하지 않는다.** 다른 영역의 변경이 필요하면 해당 담당자와 합의 후 진행한다.

## 공통 규칙

- 인증키·시크릿은 반드시 `.env`에서 읽는다. 코드에 하드코딩하거나 커밋하지 않는다.
  - `DATA_GO_KR_KEY` = 공공데이터포털 공통 인증키 (충전소·날씨·대구 교통·관광 등 data.go.kr API 전부)
  - `TMAP_APP_KEY`, `KAKAO_REST_KEY` = 개별 발급 키 / `DAEGU_PARKING_KEY` = 대구 주차정보시스템 별도 키 (미발급)
- 커밋 메시지는 `[영역] 내용` 형식. 예: `[data] 충전기 상태 수집 스케줄러 추가`
- 브랜치: `feat/<영역>-<기능>` → `dev` → `main`
- 영역 간 인터페이스(API 명세, DB 스키마, 데이터 포맷)를 바꿀 때는 변경 내용을 문서화하고 관련 담당자의 확인을 받는다.

## 핵심 도메인 규칙 (전 영역 공통)

모든 영역에서 일관되게 사용해야 하는 기준값이다. 임의로 바꾸지 말 것.

### 추천 점수 가중치

```
최종 추천 점수 = 충전 가능성 40% + 상태 정보 신뢰도 20% + 이동시간 15%
              + 충전기 수·대기 위험 15% + 주차·운영·주변 편의성 10%
```

### 상태 갱신 시각 → 신뢰도 등급

| 마지막 갱신 이후 경과 | 등급 |
|---|---|
| 5분 이내 | 신뢰도 높음 |
| 5~15분 | 신뢰도 보통 |
| 15분 이상 | 상태 확인 필요 (사용자에게 경고 표시) |
| 장시간 미갱신 | 추천 점수 감소 |

### 실패 위험도

`낮음 / 보통 / 높음` 3등급. 산출 요소: 사용 가능 충전기 비율, 전체 충전기 수, 도착 예상시간, 상태 갱신 시각, 고장·점검 충전기 수, 교통 혼잡도, 운영시간.

- 충전기 1대뿐, 상태 갱신 오래됨, 이동시간 김 → 위험도 상승
- 충전기 다수, 최근 갱신, 이동시간 짧음, 고장 비율 낮음 → 위험도 하락

## MVP 우선순위

지금은 MVP 범위(README의 "MVP 범위" 섹션)만 구현한다. 예측 모델, 예약, 리뷰 등 확장 기능을 미리 만들지 않는다. 규칙 기반 점수 계산으로 시작한다.

## 외부 API 주의사항

- 공공데이터포털 API는 일 트래픽 제한이 있다(EvCharger 기준 1,000건/일). 개발 중 불필요한 반복 호출을 피하고, 수집된 데이터를 재사용한다.
- 신규 발급 인증키는 동기화까지 약 1시간 걸린다. 403 Forbidden이면 키 동기화 여부부터 의심한다.
