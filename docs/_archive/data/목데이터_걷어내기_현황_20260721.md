# Mock 걷어내기 현황 (DA➀)

| | |
|---|---|
| **작성** | 2026-07-21 |
| **담당** | AI·데이터 ① |
| **목적** | D1에서 가짜 피처를 어디까지 뺐는지 |

> **실 vs 목 트랙** → [`실데이터_목데이터_트랙.md`](../../data/운영/실데이터_목데이터_트랙.md)  
> **수집 루프** → [`수집루프_쉬운설명.md`](../../data/운영/수집루프_쉬운설명.md)

---

## 0. 한 줄

> **D1에 mock 거리를 넣지 않는다.**  
> 돌발 = UTIC 실조인 · 주차 = 컬럼만 두고 **거리 null** (KOTSA 7/23 대기).

---

## 1. D1 플래그 (재빌드 후)

| 플래그 / 컬럼 | 값 | 의미 |
|---|---|---|
| `traffic_is_mock` | **`false`** | UTIC |
| `traffic_source` | **`utic`** | |
| `nearest_incident_m` | 실거리 (1km 매칭분) | mock join **미사용** |
| `parking_is_mock` | **`true`** | 실주차 없음 |
| `parking_source` | **`none`** | |
| `nearest_parking_m` | **전부 null** | mock 주차 거리 **걷어냄** |
| `eta_minutes` | null | TMAP/백엔드 |

빌더: `build_d1_snapshot.py` — parking/traffic **mock CSV를 D1에 merge하지 않음**.

---

## 2. 원본 mock CSV는?

| 위치 | 처리 |
|---|---|
| `docs/data/extracted/*_mock.csv` | **보관** (스키마·SANDBOX clean용) · D1 피처 아님 |
| `spatial_join/join_parking_mock_*.csv` | 동일 · D1 미투입 |
| `spatial_join/join_traffic_incident_mock_*.csv` | D1 폴백 **제거** (UTIC 없으면 null) |
| `join_traffic_incident_utic_1000m.csv` | **D1 돌발 정본** |

---

## 3. 주차 실데이터는 언제?

KOTSA **7/23** 업데이트 후 재프로브 → extract → join →  
`parking_source=kotsa` · `nearest_parking_m` 채움 · `parking_is_mock=false`.

PIS(AWS)·카카오는 보류.

---

## 4. 관련

| 문서 | 경로 |
|---|---|
| 루프 | [`수집루프_쉬운설명.md`](../../data/운영/수집루프_쉬운설명.md) |
| 피처 | [`피처_카탈로그.md`](../../data/스키마/피처_카탈로그.md) |
| 데이터셋 | [`데이터셋_명세.md`](../../data/스키마/데이터셋_명세.md) |

```
DA➀ | mock strip from D1 | 2026-07-21
```
