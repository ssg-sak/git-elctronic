# EV SafeCharge 시계열 예측 종합 보고서 (갱신)

```
SSG-SAK  |  DA➀ 이현석  |  2026-07-28
문서 유형: 수집 현황 및 타당성 게이트 (7월 28일 기준 갱신)
선행 정본: docs/보고/시계열예측_종합보고서_20260723.md
```

| 항목 | 내용 |
|---|---|
| **현 판정** | **CONDITIONAL GO** (유지) |
| **핵심 질문** | ETA(도착 시각)에 **실제로 사용 가능한** 충전소를 데이터로 말할 수 있는가 |
| **범위** | 2026-07-17 수집 시작 → 07-22 5분 서버 전환 → 07-28 기준 점검 |
| **원본 수정** | 없음 (검증·보고는 읽기 전용; 수집 파이프라인 코드 유지) |

---

## 0. 한 줄 결론

**변경분 status(loop1)로 ETA 15분 실관측 라벨을 생성하는 기반은 확보되었다.**  
이 문서의 수집 지표는 2026-07-28 당시 점검 기록이며, 현재 AWS 서버의 운영 상태를 나타내는 실시간 모니터링 결과가 아니다. 현재 운영 상태는 서버의 최신 로그와 산출물을 기준으로 확인한다.

---

## 1. 수집 품질 및 데이터 유효성 점검 기록 (2026-07-28 기준)

오늘(07-28) 실행한 `check_collection_health.py` 및 `validate_data_validity.py` 점검 결과입니다.

| 지표 | 07-23 (이전) | **07-28 점검값** | 비고 |
|---|---|---|---|
| 누적 스냅샷 수 | 1238 | **1,254** | 당시 누적값 |
| 상태(loop1) 최신성 | gap median 5.02분 | **171.2분** | 당시 점검값 |
| 교통(loop3) 최신성 | 연속 적재 | **174.8분** | 당시 점검값 |
| 25분+ 공백(R2) | 0회 (개선됨) | **발생** | 당시 점검값 |
| D1 - Status Drift | - | **153.0분** | 당시 점검값 |



---

## 2. 예측(시계열) 관점에서의 현재 상태

```text
[가능]  ETA15 실관측 라벨 생성 (07-23 기점)
[가능]  시간순 train/valid/test 분할
[가능]  패널 가용률·status_age 피처
[진행]  8월 9일까지의 캘린더 일수 묵묵히 축적
[미완]  소통 링크맵 ↔ 충전소 공간 조인
```

### 재게이트 체크리스트 (8월 9일 이후 실행)

1. **8월 9일 데이터 수집 완료 후** D1 스냅샷 및 시계열 패널 최종 병합
2. 수집 공백이 확인될 경우 **부분적 라벨링(Partial Labeling)** 또는 **ffill(Forward Fill)** 한계 돌파 방안 모색
3. ETA15 라벨 재생성 + 시간순 백테스트 재실행
4. 규칙 MVP 점수와 ML(GradientBoosting) Brier Score 비교 검증

---

## 3. 권고 (역할별)

| 대상 | 지금 (07-28) | 8월 9일 이후 |
|---|---|---|
| **DA➀** | 현 수집 로직 유지 (코드 수정 금지). 과거 이용이력 조인 등 오프라인 피처 준비 | 8월 9일 누적 데이터로 최종 D1 빌드 및 재게이트 |
| **제품/②** | 규칙 MVP 집중. ML 점수는 8월 9일 이후 데이터 확보 시까지 보류 | 최종 라벨을 통한 ML 타당성 점검 |
| **인프라** | 서버 최신 로그·수집 산출물 기준으로 운영 상태 확인 | 필요 시 수집 품질 점검 |

---

## 4. 종합 판정 문구 (팀 공유용)

> **CONDITIONAL GO — 실관측 라벨과 시계열 예측 검증을 계속 진행합니다.**  
> 이 문서의 수치는 2026-07-28 점검 기록이며, 현재 AWS 운영 상태에 대한 판정은 최신 서버 로그와 산출물로 별도 확인해야 합니다.  
> 수집된 데이터로 최종 D1 빌드, ETA15 라벨 재생성, 시간순 백테스트와 규칙 MVP·ML 비교를 진행합니다.

```
DA➀ | timeseries prediction synthesis (UPDATED) | 2026-07-28
PC 15·10분 → Lightsail 5분 → 2026-07-28 점검 기록
```
