# 데이터 품질 — 개선 적용 (2026-07-23)

점검 원본: [`데이터품질_점검_20260723.md`](./데이터품질_점검_20260723.md)  
실행: `python apps/data-pipeline/processing/analysis/fix_dq_artifacts.py`  
메타: `docs/data/analysis/dq_check_20260723/fix_applied.json`

| 문제 | 조치 | 결과 |
|---|---|---|
| 좌표 오염 | quarantine CSV + **service** CSV (`coord_ok`∧`delYn≠Y`) | quarantine **27행 / 9소** · service **24,554행 / 4,141소** |
| hours≠전체 | info 정본으로 **전 충전소 hours** 재생성 | **4,210**소 · 예전 496은 `*_partial496_*.csv` 백업 |
| live 단절 | archive 스냅샷 **201개** → `loop1/snapshots` 복사 | live 합계 **553** (=분석 합본) |
| archive 누락 읽기 | `loop_paths.status_snapshots_dirs()`에 archive 포함 + 파일명 dedup | 로더가 live 우선 |
| info.`stat` 오용 | service CSV에서 `stat` 제거 · `stat_at_info_fetch`만 유지 | 라이브는 loop1 |
| `delYn=Y` | service / hours에 `is_service_target` | PG 로더 기본 service 선호 |
| 주차 mock | `extracted/parking/PARKING_MOCK_DO_NOT_SCORE.md` | 점수 금지 명시 |
| `chgerType=11` | cleansing 매핑 잠정명 | 초급속(미확인규격) |

## 쓰라고 바뀐 파일

| 용도 | 경로 |
|---|---|
| 거리·지도·ETA 후보 | `extracted/charger/info/daegu_charger_info_service_latest.csv` |
| 좌표 이상 목록 | `…/daegu_charger_info_quarantine_coords_latest.csv` |
| 플래그 포함 전체 | `…/daegu_charger_info_flagged_latest.csv` |
| 영업시간 전체 | `extracted/charger/hours/daegu_charger_hours_full_latest.csv` (= latest) |
| status live | `docs/data/loops/loop1/snapshots/` (553) |

PG 테스트 적재: `load_charger_info_to_pg.py`는 **service_latest 우선**, hours는 **full** 우선.

```
DA① | DQ fixes applied | 2026-07-23
```
