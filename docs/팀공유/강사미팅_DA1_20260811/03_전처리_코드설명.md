# 03. 전처리 · 코드 설명 (강사 미팅)

원본 CSV 불변. 산출 표만 생성.

## 파이프라인

1. dedupe `(statId, chgerId)` → 최신 `statUpdDt`
2. `stat` → 표준 5값
3. gap-safe: 간격 **25분 초과**면 끊고 ffill
4. 충전소 집계 · 가용률 = 가능/(가능+사용중)
5. 현재표: `statUpdDt ≤ as_of` · 신선도 5/15 · `coord_ok`

## 상태 매핑

| `stat` | 표준 |
|---|---|
| 1 | UNKNOWN |
| 2 | AVAILABLE |
| 3 | IN_USE |
| 4 | OUT_OF_ORDER |
| 5 | UNDER_INSPECTION |
| 9·없음 | UNKNOWN (≠고장) |

정본: `docs/data/스키마/상태코드_매핑.md`

## 코드 위치

| 단계 | 파일 |
|---|---|
| dedupe | sandbox `load_snapshots.py` |
| gap-safe | `build_panel.py` · `gap_safe_panel` |
| 집계 | `build_d2_panel.py` |
| 현재표 | `build_d1_snapshot.py` |
| 신선도 | `processing/core/reliability.py` |
| 매핑 | `processing/core/cleansing.py` |

```
강사미팅 03 | 전처리·코드
```
