# DA② 질문·확인사항 — 2026-08-07 (최종 전 사전 검증)

| 항목 | 내용 |
|---|---|
| **작성** | AI·데이터 ① |
| **수신** | AI·데이터 ② (모델·평가·서빙 / 조장) |
| **목적** | 8/9 최종 패키지 전에 계약·타겟·피처를 맞춘다 |
| **관련** | [`핵심갭_개선계획_20260806.md`](./핵심갭_개선계획_20260806.md) · [`파생변수_검토_도착라벨_20260806.md`](./파생변수_검토_도착라벨_20260806.md) · [`최종패키지_조장전달_목록_20260809.md`](./최종패키지_조장전달_목록_20260809.md) |

---

## ①이 이미 고정한 것 (확인만 해 주세요)

| # | 항목 | ① 제안 | ② 확인 |
|---|---|---|---|
| 1 | 주력 학습 타겟 | `target_available_at_arrival` (ETA 연동 도착 가용) | [ ] |
| 2 | 보조 벤치마크 | 고정 10/15/30 `arrival_availability_replay` | [ ] |
| 3 | 평가 지표 | accuracy 금지 · **PR-AUC / negative recall / Brier** | [ ] |
| 4 | MVP 피처 | A세트 + `single_charger` + (`horizon`\|`eta_bucket` 택1) | [ ] |
| 5 | `eta_is_proxy` | **평가 층화만** · 학습 입력 금지 | [ ] |
| 6 | usage / 주차 점수 | HOLD_SPARSE · AUX only · 점수 금지 | [ ] |
| 7 | freshness | `observation_state`/age 게이트 · 등급 완화 금지 | [ ] |
| 8 | 서빙 ETA | 최종 3~5는 **BE TMAP** · ① ETA 테이블은 학습·오프라인용 | [ ] |
| 9 | 전달물 | 데이터+원천+재현코드 zip (md만 금지) | [ ] |

---

## ②에게 묻는 것 (답 필요)

### A. 타겟·샘플

1. 후보 필터(출발 시점 `available_count>0`)를 **유지**할까요, 아니면 “출발 비가용 포함” 실험도 원하나요?  
   - 유지 시 양성률 ~0.97–0.98 · persistence 강함  
2. 토이 로지스틱 ROC≈0.74 갤러리는 **무시**해도 될까요? (운영 모델 아님)

### B. ETA

3. 8/9 전까지 TMAP 실측이 1848 전부가 아니어도(프록시 잔존) **학습 진행**할까요?  
4. 학습 시 `eta_is_proxy==true` 행을 **제외 / 가중↓ / 전부 포함+층화** 중 어떤 정책이 좋나요?

### C. 피처

5. `horizon_minutes` vs `eta_bucket` — 기본은 **horizon**으로 둘까요?  
6. HOLD인 `avail_ratio_t0`·오래됨 플래그를 8/9 ablation에 **넣을까요, 빼 둘까요?**

### D. 패키지·일정

7. 8/9 전달 zip에 **원천 CSV 전체(04)** 가 필요합니다. 용량 큰데 받으실 PC 여유 있나요?  
8. D2 패널이 저녁 재빌드에서 자주 느립니다. 8/9 최종 때 **parquet만**으로 가도 될까요? (CSV 생략)

### E. 경계

9. 규칙 점수 가중치·실패위험 등급 문구는 **②**가 씁니다. ① 문서의 가중치표는 참고만으로 두면 될까요?  
10. Supabase/DB 적재는 BE 범위로 보고, ①은 파일 핸드오프만 해도 될까요?

---

## ① 쪽 미결 (8/8–8/9에 처리)

| 항목 | 상태 | 비고 |
|---|---|---|
| D2 패널 → 8/7+ | 미완 | 8/6 08:16에서 멈춤 · 8/9에 parquet 우선 재빌드 |
| TMAP 잔여 실측 | 부분 | 859+α · 날짜 넘김 resume 로직 수정됨 · 쿼터 보며 재개 |
| tick 패널·replay 8/7 stamp | 미완 | D2/tick 후 재생성 |
| 도착 라벨·derived 재부착 | 대기 | 패널·ETA 갱신 후 |
| 최종 zip | 8/9 | `pack_da1_lead_handoff` 경로 stamp 맞추기 |

---

## 한 줄 회신 템플릿 (②용)

```text
A1 필터: 유지 / 확장
B3 프록시 학습: 진행 / 대기
B4 프록시 정책: 제외 / 가중↓ / 포함+층화
C5 ETA 피처: horizon / eta_bucket
D7 원천 zip: OK / 가공본만
D8 D2 CSV: parquet만 OK / CSV 필요
기타:
```

```
DA① | DA2 questions pre-final | 20260807
```
