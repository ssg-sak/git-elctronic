# ① → ② 데이터 핸드오프 | 2026-07-20

| | |
|---|---|
| **From** | AI·데이터 ① (데이터·파이프라인) · 이현석 |
| **To** | AI·데이터 ② (모델·평가·서빙) |
| **목적** | 규칙/ML **입력 테이블** 인수 · 점수·모델은 ②가 진행 |
| **갱신** | D1 = 이중 신선도(F04b) · 10분 status loop 가동 · F08 useTime |

---

## 2026-07-22 갱신 (①) — 13:36 재빌드

| 항목 | 내용 |
|---|---|
| **D1 `as_of_ts`** | **2026-07-22T13:36:33+09:00** |
| D1 행 | 4,208소 · coord_ok 4,199 |
| 관측 가용률 mean | **0.633** (전체) · 공용 **0.699** |
| 확정 가용 소 | 전체 **69.6%** · 공용 **65.4%** (1,213/1,854) |
| 돌발 | `traffic_source=utic` · mock=false · 매칭 **806**소 |
| 주차 | `parking_source=team5_pis` · `parking_is_mock=false` · 1km 실조인 |
| **이용강도 → D1 merge 완료** | `usage_level` 등 · `history_observed` **200**소 (공용 192) |
| 조인 | 대구시 이용현황 → `statId` · 80m · 201/219 |
| **이용 제한 필터** | 공용 기본 **1,854**/4,208 · [`이용제한_필터.md`](../data/가이드/이용제한_필터.md) |
| 안내 | [`다음할일_1_공용집계_2_이용강도.md`](../data/품질보고/다음할일_1_공용집계_2_이용강도.md) · [`KPI_보고서.md`](../data/운영/KPI_보고서.md) |

> **② 참고:** `usage_level`(적음/보통/많음)은 **보조 신호**. 실시간 가용을 덮어쓰지 말 것. 감점 크기는 합의.  
> **FE/②:** 기본 후보 = `recommend_public_default=true`.

최신 D1: `.../datasets/station_feature_snapshot_latest.csv|.parquet`  
이번 빌드: `station_feature_snapshot_20260722_133820.*`

---

## 톡/회의용 복붙

```text
[①→② 핸드오프] 규칙·학습용 입력 데이터셋 넘깁니다.
점수·위험도·추천이유·모델은 ② 영역입니다.

■ 개념 요약 (1분)
docs/data/가이드/학습가이드_관측과가용.md
→ 관측=상태를 안다 / 가용=비어 있다 / 추천=점수(②)

■ 명세
- docs/data/스키마/데이터셋_명세.md
- docs/data/스키마/피처_카탈로그.md
- docs/data/스키마/상태코드_매핑.md

■ 패키지
apps/data-pipeline/evaluation/results/datasets/handoff_to_model/

■ D1 스냅샷 (충전소 1행, 4,208행, station_feature_v1)
- 최신 포인터: .../datasets/station_feature_snapshot_latest.csv|.parquet
- 이번 빌드: .../datasets/station_feature_snapshot_20260720_155425.csv|.parquet
- 샘플: handoff_to_model/station_feature_snapshot_sample_30.csv
- as_of: 2026-07-20 15:53 KST
- status: SANDBOX 시계열 as-of (관측 충전기 14,305/25,334, 가용확인 소 2,669)
- F08 is_operating_now: useTime 파서 (결측≠Y)
- **신규** F04b/F05b: observation_age_minutes, observation_grade, reliability_grade_effective
  → UI·점수는 effective 권장 (statUpdDt만 보면 CHECK 94%대)

■ status 수집 (① SANDBOX, 현재 가동)
- interval 10분 / period 15분 · PC run_loop (2026-07-20 15:55~ 재시작)

■ D2 패널 (충전소×시각, station_feature_panel_v1)
- 전체: .../datasets/station_feature_panel_20260720_100938.csv|.parquet
- 샘플: handoff_to_model/station_feature_panel_sample_50.csv
- 25분 공백 안전 forward-fill

■ 필수 정책
1) 미관측 ≠ 사용 불가 (availability_ratio_observed는 관측 0이면 null)
2) parking_is_mock / traffic_is_mock = true
3) eta_minutes = null 예약 → TMAP/백엔드가 채움
4) 점수·랭킹·위험도·추천 문장은 ②

질문·스키마 변경은 ①과 합의해 주세요.
```

---

## 인수 체크 (②)

- [ ] `학습가이드_관측과가용` · `데이터셋_명세` · `피처_카탈로그` 읽음
- [ ] D1 `station_feature_snapshot_latest` 또는 샘플 30행 스키마 확인
- [ ] D2 샘플 50행 · `panel_ts` / `segment_id` 확인
- [ ] 미관측·mock·eta 정책 동의
- [ ] 이후 점수·기준모델·평가는 ② 작업으로 진행

---

## 경로 요약

| 구분 | 경로 |
|---|---|
| 핸드오프 폴더 | `apps/data-pipeline/evaluation/results/datasets/handoff_to_model/` |
| D1 최신 | `.../datasets/station_feature_snapshot_latest.*` |
| D1 이번 빌드 | `.../datasets/station_feature_snapshot_20260720_155425.*` |
| 신선도 보고 | `docs/보고/EDA_보고서.md` §15 |
| D2 전체 | `.../datasets/station_feature_panel_20260720_100938.*` |
| 관측·가용 학습서 | `docs/data/가이드/학습가이드_관측과가용.md` |
| 상태 매핑 | `docs/data/스키마/상태코드_매핑.md` |

재생성:

```bash
python apps/data-pipeline/evaluation/personal/experiments/SANDBOX_20260716_preprocess_pipeline/src/preprocessing/build_d1_snapshot.py
python apps/data-pipeline/evaluation/personal/experiments/SANDBOX_20260717_status_periodic_collection/src/build_d2_panel.py
```
