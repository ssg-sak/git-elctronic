# 인포 커버리지 갭 — 단계 계획 (2026-07-31)

## 목표
완전 목록을 만들지 않고, **구멍의 종류를 인정·측정·모니터링**한다.

## 단계

| 단계 | 내용 | 상태 | 산출 |
|---|---|---|---|
| **0** | MVP 범위 선언: 관측 가능 EvCharger 충전소 | ✅ | 본 문서 · KPI 문구 |
| **1** | 갭 지표를 KPI에 추가 (K11–K13) | ✅ | `docs/data/운영/KPI.md` |
| **2** | 2025 신축 입주 단지 시드 × info 이름 대조 | ✅ 1차 | `신축단지_인포대조_20260731/` |
| **2b** | 2025 보강 + **2026 시드** + 단지별 충전 데이터 팩 | ✅ | `신축단지_충전데이터팩_20260731/` |
| **3** | info **일일 덤프** 동일 조건 고정 | ✅ | `dump_daily_charger_info.py` · `daily/YYYY-MM-DD/*_latest` · `run_daily_update` Step 1b · K12 |
| **4** | 시드 분기 갱신·최신 info 이름 대조 | ✅ 1차(8/3) | `신축단지_인포대조_20260803/` · 대부분 MISSING, 두류역서한포레스트=FOUND |
| **5** | (선택) 한전 등록일자 등 외부 교차 | 보류 | 필요 시 |

## 판정 라벨 (단계 2)
| 라벨 | 의미 |
|---|---|
| `FOUND_NAME` | info에 단지명과 강하게 맞는 `statNm` |
| `POSSIBLE_RELATED` | 브랜드·지역 토큰만 겹침 → 수동 확인 |
| `MISSING_IN_INFO` | 유사 이름 없음 → Type A 후보 |

## 하지 않을 것
- 결측을 0으로 채우기
- 신설 확정 피처 성급 추가
- “대구 충전기 완전 목록” 주장

## 담당
- DA①: 시드·대조·일일 info·KPI 수치
- DA②/BE: 추천 범위를 관측 가능 후보로 안내

## 코드 위치
팀 zip·신축 스크랩·일회 대조는 `apps/data-pipeline/processing/tools/share/`  
(핵심 D1/D2·KPI 리포트는 `processing/analysis` · `features` 에 유지)

### 단계 3 운영
```text
python apps/data-pipeline/processing/extract/dump_daily_charger_info.py
# 오늘 latest 있으면 skip · 재수집은 --force (~25-30 EvCharger 호출)
# run_daily_update.ps1 Step 1b 에 포함
```
K12: 전일(또는 직전) `*_latest` 대비 `statId` +/− · `daily/YYYY-MM-DD/info_dump_meta.json`
