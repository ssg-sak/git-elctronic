# 핵심 갭 개선 계획 — 2026-08-06

<!-- DA1_DONE_BANNER_20260809 -->
> **✅ DA① 완료 (2026-08-09)** · 상태: **DA1_READY_FOR_DA2_MODEL_EVALUATION**  
> 상세: [데이터파트_①_완료상태_20260809.md](../데이터파트_①_완료상태_20260809.md)


**중점 3가지** (usage HOLD_SPARSE로 메우지 않음)

1. 변경분 status 편향  
2. ETA 부재 → **실측/보정 ETA**  
3. 라벨(도착 시 가용) 간접성 → **도착 라벨 테이블**

과거 충전이력(24.10~26.03)은 **보조 prior·발표용**만. MVP 주력·실시간 available 대체 금지.

---

## 오늘(8/6) vs 내일(8/7)

| 축 | 8/6 (TMAP 제외) | 8/7 (쿼터 회복 후) |
|---|---|---|
| 변경분 편향 | ✅ D2 패널 7/17~**8/6 08:16** 재빌드 · bias 차트·팀공유 갱신 | 일상 모니터링 |
| 도착 라벨 | ✅ `--labels-only` → **2,138,025**행 / 1596소 (패널 구간 확장) | 실측 ETA 늘린 뒤 labels-only 1회 |
| ETA 실측 | ⏸ **859/1848** · 나머지 프록시 유지 | TMAP 잔여 재호출 → `eta_is_proxy=false` 상향 |
| replay 보조 | ✅ tick~8/6 · `arrival_availability_replay_20260806` (overall 도착가용 **92.6%**) | — |

---

## 1) 변경분 status 편향

| 현상 | 대응 (①) |
|---|---|
| FRESH 소수 · STALE/UNOBSERVED 다수 | `observation_state` / age를 **게이트**로 유지 (등급 완화 금지) |
| 변경분만 보이면 가용률 왜곡 | 학습·평가는 **패널(D2 hold-fill)** 기준 라벨 사용 |
| 공용 UNOBSERVED ~15% | unobserved ≠ unavailable · 후보 제외/신뢰도 하락만 |

**하지 않음:** usage로 빈 status 채우기.

**8/6 완료**

| 항목 | 값 |
|---|---|
| D2 rows | 7,552,549 · stations 3697 · ticks 2535 |
| panel range | 2026-07-17 13:41 ~ **2026-08-06 08:16** |
| bias (row / charger / panel %) | **71.1 / 52.9 / 65.8** |
| 공유 | `docs/팀공유/상태수집_패널차트_20260806/` |

---

## 2) ETA

| 단계 | 상태 |
|---|---|
| 거리대 보정 샘플 (64/64) | ✅ `ETA_보정샘플_20260806/` |
| 공용 전체 TMAP 실측 | ⚠ **859/1848** 후 API **429 QUOTA** → **8/7 재개** |
| 나머지 | ✅ 거리대 보정 프록시 (`eta_is_proxy=true`) |
| 라벨 빌드 | ✅ `arrival_labels_tmap_eta_v1` · **2,138,025**행 / 1596소 |
| 공유 폴더 | ✅ `ETA_실제_라벨_20260806/` |

**계약**

- `eta_source=tmap_routes_*` → 실측  
- `eta_source=haversine_x_band_calibration` → 프록시 (명시)  
- 서빙 최종 3~5는 BE TMAP 재호출 권장  
- **8/7:** 쿼터 회복 후 ETA만 재개 → 성공분 반영 → `--labels-only`

---

## 3) 라벨 (도착 시 가용)

| 항목 | 정의 |
|---|---|
| 이름 | `target_available_at_arrival` |
| 의미 | `feature_as_of + eta` 시각 패널에서 `available_count > 0` |
| 후보 조건 | 시점 `available_count > 0` & known (실패 위험 학습용) |
| 파일 | `arrival_labels_tmap_eta_v1.parquet` |
| 양성률 | ~0.979 — **t0 available 후보 필터의 의도된 결과**(거의 persistence). accuracy 금지 → PR-AUC / negative recall / Brier ([클래스불균형 정책](../보고/DA1_클래스불균형_평가지표_정책_20260730.md)) |

고정 10/15/30 horizon replay는 **보조**. 주력 타겟은 ETA 연동 라벨.

---

## usage 위치 (재확인)

| 됨 | 안 됨 |
|---|---|
| 상습 혼잡/한산 설명 | 실시간 available 덮기 |
| HOLD_SPARSE 유지 | MVP 주력 점수 |

---

## 파생변수 (검토 + 부착 완료)

초안 → 중복·쿼터편향 걸러 **derived_v0** 확정·데이터 부착.  
상세: [`파생변수_검토_도착라벨_20260806.md`](./파생변수_검토_도착라벨_20260806.md) · 공유 [`파생변수_derived_v0_20260806/`](./파생변수_derived_v0_20260806/)

| 판정 | 항목 |
|---|---|
| RETAIN | 단수충전기 · (택1) ETA구간 **또는** horizon |
| EXCLUDE | 여유충전기(가용대수 중복) · `eta_is_proxy` 학습입력 · 경과×ETA |
| HOLD | 가용비율(재검증 AUC≈0.45 → 유지) · 오래됨/미관측(**패널 tick 컬럼 없어 BLOCKED**) |

산출: `arrival_labels_tmap_eta_v1_with_derived.parquet` · `station_feature_snapshot_with_eta_derived_latest.*` · `derived_v0_schema.json`

---

## ②에게 넘기는 우선순위

1. 타겟: `target_available_at_arrival`  
2. ETA: `tmap_eta_min` (또는 horizon) · **`eta_is_proxy`는 평가 층화만** (학습 입력 금지)  
3. 게이트: freshness / observation_state  
4. 피처 추가: derived_v0만 (단수충전기 + horizon/ETA구간 택1)  
5. usage·주차 점수: 넣지 말 것  

```
DA① | core-gap plan | bias + ETA + arrival-label + derived_v0 | 20260806
```
