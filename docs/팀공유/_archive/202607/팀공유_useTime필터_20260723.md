# 팀 공유 — useTime(운영시간) 필터 (2026-07-23)

## 왜
거리·ETA만으로 추천하면 **문이 닫힌 충전소**가 1순위로 나올 수 있다.

## 지금 있는 것
- **전체(정본):** `docs/data/extracted/charger/hours/daegu_charger_hours_full_latest.csv` · **4,210**개소
- (`latest` / `annotated_latest`도 동일 전체셋)
- 예전 부분셋 496: `daegu_charger_hours_partial496_20260723.csv` (백업)
- 레포 대구 info와 겹치는 useTime은 일치 · **서비스는 `is_service_target=True`만**

## 추천 쪽에 부탁 (당장 1줄)
후보 정렬 **전에**:
- 도착시각이 `useTime` 밖이면 **탈락 또는 최하위**
- `비개방`·파싱실패는 **UNKNOWN** (열린 것으로 치지 말 것)
- 거리/지도 후보는 **`daegu_charger_info_service_latest.csv`** (좌표 OK · delYn≠Y)

DA➀ 파서: `processing/features/use_time.py` (`is_operating_now`)

```
DA➀ | useTime filter handoff | 2026-07-23
```
