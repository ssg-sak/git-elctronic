# 8/9 최종 패키지 — 모델·서빙 담당 조장 전달 목록

| 항목 | 내용 |
|---|---|
| **작성** | AI·데이터 ① |
| **수신** | 모델·평가·서빙 담당 (조장 / DA②) |
| **전달일** | 2026-08-09 (수집 종료·재생성 후) |
| **상태 목표** | `DA1_READY_FOR_DA2_MODEL_EVALUATION` |
| **명령 초안** | [`최종패키지_명령_초안_20260806.md`](./최종패키지_명령_초안_20260806.md) |

이 문서는 **무엇을 zip에 넣고, 조장이 무엇을 읽고/쓰면 되는지**를 고정한다.  
점수·랭킹·서빙 채택은 ② 영역이며, ①은 입력·라벨·계약·**재현 코드**까지 책임진다.

**금지:** md/설명만 보내는 것.  
**정본:** `02 데이터(parquet/CSV)` + `04 원천` + `06 재현 코드` (+ 00 계약 문서).

---

## 1. 전달물 (Desktop zip 2종)

| zip | 스크립트 | 용도 |
|---|---|---|
| `EV_SafeCharge_DA1to조장_풀데이터_20260809_*.zip` | `pack_da1_lead_handoff.py` | **본팩** — 데이터 + 원천 + **코드** + 계약 |
| `EV_SafeCharge_시각화팩_20260809.zip` (또는 당일 stamp) | `pack_viz_bundle.py` | 발표·설명용 그림 (본팩 보조) |

본팩이 정본이다. 시각화팩·md만 보내면 모델 평가·인수에 부족하다.

---

## 2. 본팩 폴더 구성 (조장이 보는 구조)

```text
EV_SafeCharge_DA1to조장_풀데이터_YYYYMMDD_HHMM/
  README_먼저읽기.md          ← 맨 먼저
  00_먼저읽기/                ← 계약·핸드오프
  01_피처적합도_유의성/       ← 적합도·스키마·과적합
  02_모델테스트_가공본/       ← 학습·현재표·시간표·라벨·ETA
  03_커버리지_주차/           ← 한계 문서
  04_원천수집_CSV/            ← 재현용 원천
  05_품질리포트/              ← 게이트·추이
  06_재현코드_DA1/            ← ★ 파이썬·스크립트 (키 제외)
  pack_meta.json
```
---

## 3. 반드시 넣을 것 (체크리스트)

### 3-A. 먼저 읽기 (`00_먼저읽기`)

| 파일 | 왜 |
|---|---|
| `팀공유_핸드오프_①to②_20260809.md` | 당일 as_of·계약 한 장 (8/4 문서를 8/9로 갱신) |
| `핵심갭_개선계획_20260806.md` | 편향·ETA·도착라벨 중점 |
| `파생변수_검토_도착라벨_20260806.md` | derived_v0 RETAIN/EXCLUDE |
| `파생변수_derived_v0_20260806/` | 부착 결과·스키마 |
| `충전이력_usage_희소성_과적합_*.md` | HOLD_SPARSE |
| `주차_realtime_428_한계_*.md` | 주차 점수 금지 |
| `D1_KPI_핸드오프_*.md` | 숫자 요약 |
| `데이터파트_①_8월9일까지_로드맵.md` | 범위·완료 정의 |

### 3-B. 모델 바로 쓸 데이터 (`02_모델테스트_가공본`)

| 파일 | 역할 |
|---|---|
| `station_feature_snapshot_latest.*` | **현재표** — 지금 기준 충전소 1행 (규칙 MVP 입력) |
| `station_feature_snapshot_with_eta_derived_latest.*` | 현재표 + ETA |
| `station_tmap_eta_latest.csv` | 충전소별 ETA (동대구 고정 origin · 학습용) |
| `arrival_labels_tmap_eta_v1_with_derived.parquet` | **주력 타겟** `target_available_at_arrival` |
| `derived_v0_schema.json` | 파생 계약 (추가 파생 불필요 확정) |
| `station_horizon_training_v1.parquet` | 고정 도착시차 학습(보조) |
| `station_feature_panel_latest.parquet` | **시간표** — 충전소×시간 상태 |
| `station_tick_panel.parquet` | 틱 단위 복원표 (과거 재생용) |
| `handoff_to_model/` | 현재표·시간표 메타·샘플 |
| `arrival_availability_replay_20260809/` | 고정 10/15/30 보조 replay |
| `kpi_report_latest.json` | KPI 스냅 |

> 8/9 재생 전 mid 스탬프(0804/0806)가 남아 있으면 **당일 재생성분으로 교체**한 뒤 팩킹한다.

### 3-C. 피처·품질 (`01` · `05`)

| 항목 | 경로 예 |
|---|---|
| 적합도·과적합 추천 | `피처적합도_비교_20260809/` (또는 최신) |
| HGB/스키마 리포트 | `hgb_training_pipeline_20260809/` |
| monitor / validate | `recommendation_input_monitor_latest.json` 등 |
| dedupe · 일별 추이 · horizon overlap | `*_20260809.csv` |

### 3-D. 원천 (`04_원천수집_CSV`) — 재현·감사

- loop1 status snapshots / daily  
- loop2 UTIC · loop3 소통  
- info 일일 덤프 · usage · spatial_join · Team5 주차  

용량 큼. 조장 PC 디스크 여유 확인.

### 3-E. 재현 코드 (`06_재현코드_DA1`) — 필수

문서만 넘기고 끝내지 않는다. **①이 만든 실행 코드를 같이 넣는다.**  
(`pack_da1_lead_handoff.py`가 자동 복사 · `.env`/시크릿 제외)

| 묶음 | 내용 |
|---|---|
| `processing_analysis/` | ETA·도착라벨·derived_v0·replay·품질 게이트·KPI |
| `processing_features/` · `processing_extract/` | 피처·일일 덤프·조인 |
| `sandbox_d1_src/` · `sandbox_d2_status_src/` | 현재표·시간표 만드는 코드 |
| `evaluation_feasibility/` · `evaluation_tests/` | tick 패널 · pytest |
| `scripts/` | Lightsail pull · evening rebuild 등 |
| `00_가이드/` | AGENTS · requirements · 최종 명령 초안 |

조장이 repo 없이도 “무엇을 돌렸는지” 보고, 가능하면 같은 명령을 재현할 수 있어야 한다.

### 3-F. 시각화팩 (별 zip)

상태 패널 bias 차트 · 가용률 · 혼잡 · IQR · UTIC 등.  
모델 학습 필수는 아님.

---

## 4. 조장이 읽는 순서

1. `README_먼저읽기.md`
2. `최종패키지_조장전달_목록_20260809.md` (본 문서 복사본)
3. `팀공유_핸드오프_①to②_*.md`
4. **학습 타겟:** `02/.../arrival_labels_tmap_eta_v1_with_derived.parquet`
5. **규칙 MVP 입력:** `station_feature_snapshot_latest` (+ with_eta_derived)
6. **코드:** `06_재현코드_DA1/README_코드.md`
7. 보조: horizon training · fixed replay · 적합도
8. 막히면 `04_원천` / `05_품질`
---

## 5. 계약 — 써도 되는 것 / 쓰면 안 되는 것

### 써도 됨

| 항목 | 비고 |
|---|---|
| 타겟 `target_available_at_arrival` | PR-AUC / negative recall / Brier (accuracy 금지) |
| **최종 9피처** (8/8 확정) | `available_count`, `total_chargers`, `known_charger_count`, `observation_coverage`, `hour`, `weekday`, `avail_rate_lag_15m`, `avail_rate_lag_60m`, **`tmap_eta_min`** |
| `observation_state` / age **게이트** | 등급 완화 금지 · 학습 피처와 별개 |
| `eta_is_proxy` | 현재 실측 전량(0) · 있어도 **평가 층화만** |
| 시간표 기준 가용 | 변경분 raw 평균으로 학습하지 말 것 |
| HGB | 모델 패밀리 확정 기준 (적합도·과적합 PASS) |

### 쓰면 안 됨 (점수·주력 피처)

| 항목 | 이유 |
|---|---|
| usage 이력 3종 | HOLD_SPARSE · 과적합 |
| 주차 realtime → 점수 | 428·커버 한계 · AUX only |
| `eta_is_proxy`를 학습 입력 | 쿼터 선택편향 |
| `single_charger` / `eta_bucket` / `avail_ratio_t0` | 8/8 HGB 증분 없음 · 불필요 |
| ① 동대구 ETA를 서빙 ETA로 사용 | **고정 origin 편향** · BE TMAP만 |
| 여유충전기 (=가용−1) | 중복 |
| 요금·좌표를 점수 피처 | 기존 계약 |
| 현재표 `eta_minutes` null을 “고장”으로 해석 | null 예약 · ETA 붙인 Companion 파일 사용 |

### 서빙

- 최종 후보 3~5 **경로 ETA = 백엔드 TMAP 재호출**이 정본  
- ① ETA 테이블은 학습·오프라인 평가용

### ⚠ ETA 동대구역 고정 (필독)

> ① `station_tmap_eta_latest` / 도착라벨의 ETA·horizon은 **동대구역(dongdaegu) origin 하나**로 뽑았다.  
> **지리 대리 편향 가능** → 런타임 랭킹 ETA로 쓰지 말 것.  
> 전문: [`ETA_동대구고정_한계_조장필독_20260808.md`](./ETA_동대구고정_한계_조장필독_20260808.md)

---


## 6. 8/9 당일 생성 순서 (요약)

상세 명령은 [`최종패키지_명령_초안_20260806.md`](./최종패키지_명령_초안_20260806.md).

```text
Lightsail pull
 → evening rebuild (현재표·KPI·차트)
 → 시간표 재빌드
 → 품질 게이트
 → 과거재생 · 고정시차 학습
 → ETA/도착라벨 갱신
 → 핸드오프 md stamp 20260809
 → pytest
 → pack zip 2종
 → D: 또는 Desktop 전달
```

**팩킹 전:** `pack_da1_lead_handoff.py` 안의 날짜 경로를 **8/9 산출물 경로로 갱신**할 것.

---

## 7. 전달 완료 기준 (① 자체 검수)

- [ ] 수집 구간 ≥ 14일 (7/17 시작 → 8/9 충족)
- [ ] validate FAIL 0 · monitor PASS · pytest 통과
- [ ] 시간표 / 틱표 끝시각 ≈ 8/9 최종 스냅
- [ ] 도착 라벨 파일 존재 · 최종 9피처 계약 일치
- [ ] 핸드오프 기준시각 = 당일 현재표
- [ ] zip 2종 생성 · `pack_meta.json`에 missing 최소화
- [ ] 재현코드 폴더 존재 · `.env` 미포함
- [ ] 상태 문구: **`DA1_READY_FOR_DA2_MODEL_EVALUATION`**

이 상태는 **데이터셋 전달 가능**이지, 모델 성능·운영 채택 보장이 아니다.

---

## 8. 조장에게 같이 적을 한 줄 메시지 (초안)

```text
DA① 최종팩 전달합니다. (문서만이 아니라 데이터+재현코드 포함)
- 상태: DA1_READY_FOR_DA2_MODEL_EVALUATION
- 02: 도착라벨 · 현재표(지금 1소1행) · 시간표(소×시간) · ETA · 최종9피처
- 06: 재현 코드 (.env 제외)
- 주력 타겟: target_available_at_arrival
- 피처(9): available_count, total_chargers, known_charger_count, observation_coverage, hour, weekday, lag15, lag60, tmap_eta_min
- 모델: HGB (적합도·과적합 PASS)
- ⚠ 학습 ETA는 동대구역 고정 origin → 서빙 최종3~5는 반드시 BE 사용자위치 TMAP
- 금지: usage/주차점수/eta_is_proxy학습입력/①ETA를런타임ETA로사용
- zip: D: 또는 Desktop
- 필독: ETA_동대구고정_한계_조장필독_20260808.md
```
```
DA① | final package checklist for lead | stamp 20260809 | updated 20260808
```
