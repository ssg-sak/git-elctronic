# 프론트엔드 환경변수 예시 — 실제 값은 .env.local에 작성 (커밋 금지)
# 주의: 외부 API 키(카카오 REST, TMAP 등)는 절대 여기에 넣지 않는다. 외부 API는 백엔드 경유.

# 우리 백엔드 API 주소
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# 지도 SDK JavaScript 키 (클라이언트 사용이 전제된 키만 허용, 도메인 제한 필수)
NEXT_PUBLIC_KAKAO_MAP_JS_KEY=
